doctype html
head
  include headTemplate.pug
  title Schedule list
  link(rel='stylesheet' href='schedule.css')
  script(defer src='schedule.js')

include sideNavi.pug

button#toCalendarButton 달력
h2
  | Schedule List (
  span#titleDate #{dateId}
  | )
form(action='/calendar/db_write' method='post' onsubmit='return inputCheck(this)')
  input(type='hidden' name='dateId' value=`${dateId}`)
  input#scheduleInput(type='text' name='schedule' autofocus='')
  input#save(type='submit' value='저장')
#scheduleList
br
br
h2 몸무게 입력
form(action='/diet/db_write' method='post' onsubmit='return inputCheck(this)')
  input(type='hidden' name='dateId' value=`${dateId}`)
  input(type='number' step='0.1' name='weight' placeholder='목표는 50kg 대...!')
  input(type='submit' value='저장')
#weightList
  | 오늘의 몸무게 = 
  span#weight
